server:
  port: 8080

spring:
  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.security.servlet.UserDetailsServiceAutoConfiguration

  datasource:
    url: jdbc:mysql://localhost:3306/money_transfer_system?allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=UTC
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: "Cloud@123$"
    hikari:
      maximum-pool-size: 5

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true

  sql:
    init:
      mode: never

  mail:
    host: smtp.gmail.com
    port: 587
    username: samyukthajagannath@gmail.com
    password: yjxq yqrf ctwl fruh
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

app:
  jwt:
    secret: MySuperSecretKeyForJWTTokenGenerationAndValidationInMoneyTransferSystem2024
    expiration: 86400000

# Snowflake configuration (placeholder defaults - update with real credentials when needed)
snowflake:
  url: "jdbc:snowflake://<your-account>.snowflakecomputing.com"
  user: "<your-username>"
  password: "<your-password>"
  role: "SYSADMIN"
  warehouse: "COMPUTE_WH"
  database: "PAYFID_DB"
  schema: "PUBLIC"

logging:
  level:
    com.company.mts: DEBUG
    org.hibernate.SQL: DEBUG